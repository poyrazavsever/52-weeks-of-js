# 🎯 Implementation Plan - The Engineering Redemption Platform

**Project:** 52 Weeks of JavaScript - Web Platform  
**Author:** Poyraz Avsever  
**Date Created:** February 16, 2026  
**Last Updated:** February 17, 2026  
**Status:** ✅ Phase 1-5 Complete (Resources & Assets Pages Added)

---

## 📊 Project Status Overview

**Completed Components:**

- ✅ Button Component (Dashed border, variants, sizes, states)
- ✅ Navbar Component (Fixed header, dropdown menu with icons, animations)
- ✅ Root Layout Structure
- ✅ **Resources Page** - Books, Tools, Weekly Resources
- ✅ **Extra Page** - Topic-based extra notes with tabbed interface
- ✅ **Assets Page** - GitHub-linked asset browser with file type icons

**Technology Stack:**

- Next.js 16.1.6 (App Router)
- React 19.2.3
- TypeScript 5.x (Strict mode)
- Tailwind CSS 4.x
- Framer Motion 12.34.0
- @iconify/react 6.0.2

**Completion:** ~75-80% (Phase 1, 2, 3 & 5 Complete)

---

## 🗓️ PHASE 1: Content Infrastructure ✅ COMPLETED

**Summary:** Content processing infrastructure is now ready. All necessary packages installed, file system API created, and MDX parser configured with syntax highlighting.

**Completed Tasks:**

- ✅ MDX packages installed (next-mdx-remote, gray-matter, rehype-pretty-code, shiki, remark-gfm)
- ✅ File System API implemented (lib/api.ts) - All read functions working
- ✅ MDX Parser configured (lib/mdx.ts) - Syntax highlighting ready

---

### ✅ Task 1: MDX & Content Paketlerini Yükle

**Estimated Time:** 5 minutes  
**Status:** ✅ COMPLETED

**Installed Packages:**

- ✅ next-mdx-remote 6.0.0
- ✅ gray-matter 4.0.3
- ✅ rehype-pretty-code 0.14.1
- ✅ shiki 3.22.0
- ✅ remark-gfm 4.0.1
- ✅ @types/mdx 2.0.13 (dev)

---

### ✅ Task 2: File System API Oluştur

**File:** `website/lib/api.ts`  
**Estimated Time:** 30 minutes  
**Status:** ✅ COMPLETED

**Implemented Functions:**

- ✅ `getPhases()` - Returns all 4 phases with their weeks
- ✅ `getWeeksByPhase(phaseSlug)` - Gets weeks for a specific phase
- ✅ `getWeekContent(phaseSlug, weekSlug)` - Reads README.md and parses metadata
- ✅ `getLabFiles(phaseSlug, weekSlug, labNumber)` - Reads lab code files
- ✅ `getResourcesContent()` - Returns books, tools, weekly resources
- ✅ `getExtraContent()` - Returns extra notes and lab content

**Key Features:**

- ✅ Automatic metadata extraction from Markdown
- ✅ Topic and goal parsing from README structure
- ✅ Lab and notes folder detection
- ✅ Language detection from file extensions
- ✅ Complete TypeScript type definitions

---

### ✅ Task 3: MDX Parser Yapılandır

**File:** `website/lib/mdx.ts`  
**Estimated Time:** 30 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

- ✅ `parseMDX(content)` - Main parsing function
- ✅ `parseMDXFromFile(filePath)` - File-based parsing
- ✅ `parseMDXWithTheme(content, theme)` - Custom theme support
- ✅ Rehype Pretty Code integration (VS Code-quality highlighting)
- ✅ Remark GFM support (tables, task lists)
- ✅ Multiple theme options available

**Configuration:**

- Theme: `github-dark-dimmed` (default)
- Line highlighting support
- Word highlighting support
- Empty line preservation

---

## 🗓️ PHASE 2: Dynamic Routing & Content Display ✅ COMPLETED

**Summary:** Dynamic routing system is fully functional. Users can now browse all phases and weeks, view content with syntax highlighting.

**Completed Tasks:**

- ✅ Dynamic route structure created (`/[phase]/[week]`)
- ✅ Static generation for all pages (generateStaticParams)
- ✅ Home page with phase overview
- ✅ Phase overview page (`/[phase]`)
- ✅ Week detail page with MDX rendering
- ✅ Tailwind Typography plugin integrated
- ✅ 404 pages added

---

### ✅ Task 4: Dynamic Route Yapısı

**Files Created:**

- `app/page.tsx` - Homepage with all phases
- `app/[phase]/page.tsx` - Phase overview with week list
- `app/[phase]/[week]/page.tsx` - Week detail with MDX content
- `app/[phase]/[week]/not-found.tsx` - 404 page

**Estimated Time:** 45 minutes  
**Actual Time:** ~40 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

- ✅ `generateStaticParams()` for all routes (SSG)
- ✅ SEO metadata generation
- ✅ MDX content rendering with syntax highlighting
- ✅ Lab files display (basic version)
- ✅ Topics and goal display
- ✅ Navigation breadcrumbs (inline)
- ✅ Responsive layout
- ✅ Dashed border design system
- ✅ @tailwindcss/typography integration

**Working Routes:**

- `/` - Homepage with phase grid
- `/iron-foundations` - Phase 1 weeks
- `/web-architecture` - Phase 2 weeks
- `/universal-ecosystem` - Phase 3 weeks
- `/seniority-cs` - Phase 4 weeks
- `/[phase]/week-XX-...` - Individual week pages

---

## 🗓️ PHASE 3: UI Components ✅ COMPLETED

**Summary:** All major UI components implemented with animations, responsive design, and full functionality.

**Completed Tasks:**

- ✅ Sidebar component with collapsible phases and week navigation
- ✅ Lab Viewer with tabbed interface, syntax highlighting, and copy functionality
- ✅ Breadcrumbs component for navigation trail
- ✅ All components integrated into page layouts
- ✅ Responsive design (mobile hamburger menu for sidebar)

---

### ✅ Task 5: Sidebar Component

**File:** `website/components/layout/Sidebar.tsx`  
**Estimated Time:** 40 minutes  
**Actual Time:** ~35 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

- ✅ Sticky positioning on desktop (w-80, h-screen)
- ✅ Collapsible phase sections with Framer Motion animations
- ✅ Active page highlighting (red accent for current week)
- ✅ Week indicators (Lab, Notes badges)
- ✅ Mobile responsive with floating button
- ✅ Slide-in menu animation for mobile
- ✅ Auto-expand current phase
- ✅ Dashed border design system

---

### ✅ Task 6: Lab Viewer Component

**File:** `website/components/shared/LabViewer.tsx`  
**Estimated Time:** 50 minutes  
**Actual Time:** ~30 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

- ✅ Tabbed interface for multiple files
- ✅ Copy to clipboard with visual feedback
- ✅ Download all files button
- ✅ Syntax highlighting (github-dark-dimmed theme for code blocks)
- ✅ File info display (language, line count)
- ✅ Smooth tab switching animations
- ✅ Responsive code display with scroll
- ✅ Dashed border design system

---

### ✅ Task 7: Breadcrumbs Component

**File:** `website/components/shared/Breadcrumbs.tsx`  
**Estimated Time:** 20 minutes  
**Actual Time:** ~15 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

- ✅ Dynamic breadcrumb generation from routes
- ✅ Home → Phase → Week navigation
- ✅ Hover effects with red accent
- ✅ Responsive text truncation
- ✅ Integrated in phase and week pages
  </div>
);
}

```

---

### 🍞 Task 7: Breadcrumbs Component

**File:** `website/components/shared/Breadcrumbs.tsx`
**Estimated Time:** 20 minutes
**Status:** Not Started

**Display:**

```

Home > Iron Foundations > Week 02: Scope & Closures

````

**Features:**

- Auto-generated from dynamic route
- Responsive: Truncated on mobile
- Dashed separator (design system consistency)

**Implementation:**

```tsx
interface BreadcrumbsProps {
  phase: string;
  week: string;
}

export default function Breadcrumbs({ phase, week }: BreadcrumbsProps) {
  const phaseData = await getPhaseBySlug(phase);
  const weekData = await getWeekBySlug(phase, week);

  return (
    <nav className="flex items-center gap-2 text-sm text-muted">
      <Link href="/">Home</Link>
      <span className="border-l border-dashed h-4" />
      <Link href={`/${phase}`}>{phaseData.title}</Link>
      <span className="border-l border-dashed h-4" />
      <span className="text-primary">{weekData.title}</span>
    </nav>
  );
}
````

---

## 🗓️ PHASE 4: Styling & Polish ✅ COMPLETED

### ✅ Task 9: Font Sistemi

**Files:** `website/app/layout.tsx`, `globals.css`  
**Estimated Time:** 30 minutes  
**Status:** ✅ COMPLETED

**Fonts:**

1. **Inter** (UI - Google Fonts)
   - Weights: 400, 500, 600, 700
   - Variable font preferred
2. **JetBrains Mono** (Code - Google Fonts)
   - Weights: 400, 500
   - Monospace usage
3. **Agbalumo** (Display - Google Fonts)
   - Weight: 400
   - Used for decorative elements

**Implementation Notes:**

- ✅ All fonts configured in `layout.tsx` with CSS variables
- ✅ Custom scrollbar styling applied
- ✅ Selection styling (red background)
- ✅ Font families properly cascaded
  - Monospace usage

3. **Agbalumo** (Display - Google Fonts)
   - Weight: 400
   - Used for decorative elements

**Status:** ✅ All fonts configured and working in layout.tsx

**Implementation:**

```tsx
import { Inter, JetBrains_Mono } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
```

**CSS:**

```css
@layer base {
  body {
    font-family: var(--font-inter), system-ui, sans-serif;
  }

  code,
  pre {
    font-family: var(--font-mono), monospace;
  }
}
```

---

## 🗓️ PHASE 5: Content Pages & Deployment ✅ PARTIALLY COMPLETED

### ✅ Task 10: Resources ve Extra Sayfaları

**Files:**

- ✅ `website/app/resources/page.tsx` - **COMPLETED**
- ✅ `website/app/extra/page.tsx` - **COMPLETED**
- ✅ `website/app/assets/page.tsx` - **COMPLETED (Bonus)**

**Estimated Time:** 30 minutes  
**Actual Time:** ~45 minutes  
**Status:** ✅ COMPLETED

**Implemented Features:**

**Resources Page (`/resources`):**

- ✅ MDX parsing for books.mdx, tools.mdx, weekly-resources.mdx
- ✅ Tailwind Typography styling with custom prose classes
- ✅ Dashed border design system
- ✅ Table support with proper styling
- ✅ Red-accented weekly resources section

**Extra Page (`/extra`):**

- ✅ Topic-based structure (reads from `extra/notes/` folders)
- ✅ Custom `ExtraTopics` component with tabbed interface
- ✅ Framer Motion animations for tab switching
- ✅ Main README.mdx display with introduction
- ✅ API functions: `getExtraContent()` returns `{ readme, topics[] }`

**Assets Page (`/assets`) - Bonus Addition:**

- ✅ Scans `assets/` folder recursively
- ✅ Groups files by folder (books, etc.)
- ✅ `AssetsGrid` component with file type icons
- ✅ GitHub links for each file
- ✅ File type detection (image, video, document, file)
- ✅ Color-coded cards by file type
- ✅ Hover effects and GitHub redirect

**API Updates:**

- ✅ `getExtraContent()`: Returns topic-based extra notes
- ✅ `getAssetsFiles()`: Recursive asset scanning with GitHub URLs
- ✅ `ExtraTopic` interface added
- ✅ `AssetsFile` interface added
- ✅ Fixed `.mdx` file extension detection in resources

**Component Updates:**

- ✅ Navbar dropdown updated with:
  - Icons for all menu items (hammer, web, earth, trophy)
  - Extra/Lab icon: `mdi:flask`
  - Resources icon: `mdi:book-open-variant`
  - **Assets icon: `mdi:folder-image`** (NEW)

**Bug Fixes:**

- ✅ Fixed MDX parsing error in `books.mdx` (self-closing `<img>` tag)

---

### 🎨 Task 11: Asset Pipeline

**File:** `website/scripts/copy-assets.js`  
**Estimated Time:** 20 minutes  
**Status:** Not Started

**Purpose:**

- Copy `../assets` folder to `website/public/assets`
- Preserve Markdown image links `![](/assets/img.png)`
- Run automatically before build

**Script:**

```javascript
const fs = require("fs");
const path = require("path");

const sourceDir = path.join(__dirname, "../../assets");
const targetDir = path.join(__dirname, "../public/assets");

function copyAssets() {
  if (fs.existsSync(targetDir)) {
    fs.rmSync(targetDir, { recursive: true });
  }
  fs.cpSync(sourceDir, targetDir, { recursive: true });
  console.log("✅ Assets copied successfully");
}

copyAssets();
```

**package.json update:**

```json
{
  "scripts": {
    "prebuild": "node scripts/copy-assets.js",
    "build": "next build"
  }
}
```

---

### 📱 Task 12: Responsive Optimizasyon

**Estimated Time:** 40 minutes  
**Status:** Not Started

**Checkpoints:**

- Mobile: Sidebar as hamburger menu
- Tablet: Navbar dropdown layout adjustments
- Desktop: Full sidebar + content
- Code blocks: Horizontal scroll
- Touch gestures: Smooth scroll

**Breakpoints:**

```css
/* Mobile First */
.sidebar {
  @apply fixed inset-0 z-40 transform -translate-x-full transition-transform;
}

@media (min-width: 768px) {
  .sidebar {
    @apply translate-x-0 static;
  }
}
```

---

## 📊 Time Estimation Summary

| Phase                           | Duration       |
| ------------------------------- | -------------- |
| Phase 1: Content Infrastructure | 1-1.5 hours    |
| Phase 2: Dynamic Routing        | 45 minutes     |
| Phase 3: UI Components          | 2 hours        |
| Phase 4: Styling & Polish       | 1.5 hours      |
| Phase 5: Content & Deployment   | 1.5 hours      |
| **TOTAL**                       | **~7-8 hours** |

---

## 🎯 Current Priority: Phase 1

**Next Steps:**

1. ✅ Save implementation plan
2. Install MDX packages
3. Create lib/api.ts
4. Create lib/mdx.ts

---

## 📝 Notes & Decisions

- **No Database:** All content from GitHub Markdown files
- **Static Generation:** Build-time SSG for all pages
- **Type Safety:** TypeScript strict mode throughout
- **Component Prefix:** `@/` alias points to workspace root
- **Design Language:** Dashed borders, minimal aesthetic, light theme with red accents

---

**Last Updated:** February 17, 2026
